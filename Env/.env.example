# Copy this file to .env and fill in your dev keys.
# The build script will copy Env/.env into the app bundle for Debug builds only
# (as DevSecrets.env), and SettingsStore will prime the Keychain from it on first run.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
XAI_API_KEY=
